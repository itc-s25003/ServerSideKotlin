product: jvm/app

dependencies:
  - $ktor.server.config.yaml
  - $ktor.server.core
  - $ktor.server.netty
  - $ktor.server.resources
  - $libs.logback.classic

test-dependencies:
  - $ktor.server.testHost

settings:
  ktor: enabled
  kotlin:
    serialization:
      enabled: true

